# .gitignore (Place in the root 'fpl-mern-app' directory)

# Dependencies #
################
# Ignore node_modules folders in subdirectories and potentially at the root
node_modules/
frontend/node_modules/
backend/node_modules/

# Lockfiles (usually committed, but uncomment if you have specific reasons not to)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Build & Output #
##################
# Ignore build output folders for frontend and backend
frontend/dist/
frontend/build/
frontend/.vite/
backend/dist/
backend/build/

# Logs #
########
# Ignore log files and directories
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment Variables #
#########################
# Ignore all .env files, but keep examples tracked
# Use specific paths if preferred, or broad patterns
.env*
!.env.example
!.env.development
!.env.production

frontend/.env*
!frontend/.env.example

backend/.env*
!backend/.env.example


# OS & Editor Specific #
########################
# MacOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
*.stackdump

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Other Editors / IDEs
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing #
###########
# Ignore test coverage output
coverage/
.nyc_output/

# TypeScript #
##############
# Ignore TypeScript build info files (if using TS)
*.tsbuildinfo

# Optional miscellaneous #
##########################
# Ignore NPM/Yarn cache folders (if they appear in project)
.npm
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions